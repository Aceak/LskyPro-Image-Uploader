# LskyPro Upload V2  
**适用于 Obsidian 的自动图床上传插件**

简体中文 | [English](README.md)

> 一款为 Obsidian 打造的高效图片上传工具，让您的笔记图片管理更加自动化与轻量化。  
> 支持 LskyPro V1 / V2 API、多种上传方式与批量处理。

---

## 🚀 功能亮点

- **自动上传**：粘贴图片时自动上传并替换为外链  
- **多版本支持**：完整兼容 LskyPro **V1 / V2** API  
- **多种上传方式**：支持剪贴板、拖拽、右键菜单、命令面板多入口操作  
- **批量上传**：一键上传当前笔记中所有本地图片  
- **并发上传**：支持低/中/高三种并发模式，提升批量上传效率  
- **智能处理**：可选择是否处理网络图片、自动删除源文件等  
- **多语言支持**：内置中英文切换，适配不同用户需求  


---

## 📋 快速开始

### 前置条件

使用前请确保以下内容：

1. 准备可访问的 **LskyPro 图床服务**  
2. 已启用 LskyPro 的 **API 接口功能**  
3. 已获取有效的 **访问 Token**（用户中心 → 个人令牌）  
4. 对于V2版本，需要获取正确的**存储ID**，管理员可通过管理后台查看，普通用户可以使用API获取

---

### 插件配置步骤

1. 打开 **Obsidian → 设置 → 第三方插件 → LskyPro Upload V2**  
2. 按以下顺序填写基本配置：

| 配置项 | 说明 |
|--------|------|
| **图床地址** | 例如 `https://img.example.com` |
| **访问 Token** | LskyPro 用户访问令牌 |
| **版本选择** | 选择 `V1` 或 `V2` |
| **存储 ID** | V1 可选；V2 必填，可通过 [API 获取](https://lsky-pro.apifox.cn/api-188594598) |

3. 高级选项配置：

| 配置项 | 说明 |
|--------|------|
| **剪贴板自动上传** | 启用后自动上传粘贴的图片（桌面端） |
| **附件自动上传** | 启用后自动处理附件图片（移动端） |
| **处理网络图片** | 是否处理粘贴的网络图片链接 |
| **网络黑名单域名** | 指定不处理的域名列表 |
| **修复路径** | 启用后自动修复文件路径问题 |
| **应用图片处理** | 是否应用图片处理规则 |
| **上传后删除源文件** | 上传成功后是否删除本地文件 |
| **并发模式** | 批量上传时的并发数量（低/中/高） |
| **语言设置** | 插件界面语言（自动/简体中文/英文） |

---

## 💡 使用指南

### 剪贴板自动上传
直接在编辑器中粘贴图片，插件会：
1. 自动上传至 LskyPro；
2. 上传成功后替换为 Markdown 外链：
   ```markdown
   ![image](https://img.example.com/uploads/2025/xx.png)
   ```
---

### 移动端附件自动上传
点击插入附件按钮并选中图片文件后，插件会：
1. 自动上传至 LskyPro；
2. 上传成功后替换为 Markdown 外链：
   ```markdown
   ![image](https://img.example.com/uploads/2025/xx.png)
   ```
注： 此功能未在移动端测试，可能存在兼容性问题，等后续插件上线了，再进行测试和优化。

---

### 右键菜单上传
在 **文件浏览器** 或 **编辑器中选中图片路径** 后右键点击：  
选择「上传到 LskyPro」，插件会立即上传并返回外链。

> 适合手动处理单张图片或附件上传场景。

---

### 拖拽上传
将本地图片文件拖拽至编辑器中，插件会：
- 自动检测文件类型；
- 上传后替换为外链 Markdown 图片语法。

---

### 批量上传当前文档内图片
1. 按 `Ctrl + P`（或 `Cmd + P`）打开命令面板  
2. 搜索 `Upload all images` 并执行  
3. 插件将扫描并上传所有本地图片  

> 可在设置中配置并发模式（低/中/高），显著提升批量上传速度。

---

### 批量下载图片
反向操作：将文档中的网络图片下载到本地。  
命令：`Download all images`。

---

### 启用调试模式
#### 方式一：通过配置文件启用（推荐）
1. 打开插件数据目录：  
   ```
   .obsidian/plugins/lskypro-upload-v2/data.json
   ```
2. 在文件中添加或修改以下字段：  
   ```json
   "_debug": true
   ```
3. 保存文件后，重新加载插件或重启 Obsidian ，即可开启调试模式。

#### 方式二：临时启用（不修改文件）
1. 打开控制台（`Ctrl + Shift + I` → Console）；  
2. 输入以下命令：  
   ```js
   window.__LSKY_RUNTIME_DEBUG__ = true
   ```
3. 重新加载插件后即自动进入调试模式。
---

## 🔍 LskyPro 版本说明

插件基于 [NekoTarou/lskypro-auto-upload](https://github.com/NekoTarou/lskypro-auto-upload.git) 改造，  
增强了对 **LskyPro V2 API** 的支持与自动解析机制。  
参考文档：[Lsky Pro 官方 API 文档](https://lsky-pro.apifox.cn)。

| 配置项 | LskyPro V1 | LskyPro V2 |
|--------|-------------|-------------|
| 存储配置 | 可选（若仅一个策略可省略） | 必填（需填写正确存储 ID） |
| Token 授权 | 用户令牌 | 用户令牌 |
| API 路径 | `/api/v1/upload` | `/api/v2/upload` |

---

## ⚠️ 注意事项

1. 更新插件后请 **重启 Obsidian** 以确保新功能生效  
2. 网络不稳定或 Token 失效可能导致上传失败  
3. 请妥善保管 Token，不要与他人共享  
4. 切换 LskyPro 版本后，请重新填写存储配置  
5. 上传路径由 LskyPro 端策略控制，插件不修改服务器结构  

---

## 🧭 开发计划与更新日志

### 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 并发上传 | ✅ 已完成 | 支持低/中/高三种并发模式，提升批量上传效率 |
| 上传后自动删除源文件 | ✅ 已完成 | 上传成功后可选择删除本地文件，保持库整洁 |
| LskyPro V1 / V2 自动识别 | ✅ 已完成 | 自动识别并适配不同API版本的响应格式 |
| 右键菜单上传 | ✅ 已完成 | 在Obsidian右键菜单中直接上传图片 |
| 响应结果优化 | ✅ 已完成 | 统一处理status/message/data解析逻辑 |
| 多语言支持 | ✅ 已完成 | 内置中英文切换功能 |
| 网络图片处理 | ✅ 已完成 | 可选择是否处理粘贴的网络图片链接 |

### 计划中功能

- 图片压缩选项
- 更多上传状态提示

---

## 版本更新日志

- **1.1.0**：
  - 新增：桌面端剪贴板自动上传功能
  - 新增：移动端附件自动上传功能
  - 新增：多语言支持（自动/简体中文/英文）
  - 新增：并发上传模式（低/中/高）
  - 新增：调试日志输出选项
  - 优化：响应结果解析逻辑，统一处理status/message/data字段
  - 优化：移除Node依赖，仅使用浏览器API
  - 修复：部分配置项在移动端显示异常问题
  - 修复：修复移动端兼容性问题，通过平台判断是否使用桌面端依赖
  - 修复：上传后自动删除源文件选项，使用更安全的方式处理
- **1.0.0**：
  - 初始版本，支持基础的图片上传功能
  - 新增：LskyPro V1/V2 自动识别与适配
  - 新增：批量上传当前文档内图片功能
  - 新增：批量下载图片功能
  - 新增：处理网络图片链接选项（可选择是否上传）
  - 新增：网络黑名单域名配置（避免上传指定域名的图片）
  - 新增：配置项说明文档（README.md）
  - 新增：项目信息（AUTHOR、BLOG、PROJECT）
  - 新增：许可证（MIT License）

---

## 🤝 致谢

特别感谢以下项目对本插件提供的启发：

- [NekoTarou/lskypro-auto-upload](https://github.com/NekoTarou/lskypro-auto-upload.git)
- [renmu123/obsidian-image-auto-upload-plugin](https://github.com/renmu123/obsidian-image-auto-upload-plugin.git)

---

## 👨‍💻 项目信息

- 作者：**3kk0**  
- 博客：[https://wiki.stxwer.top](https://wiki.stxwer.top)  
- 项目地址：[GitHub - Aceak/LskyPro-Upload-V2](https://github.com/Aceak/LskyPro-Upload-V2)

## 📜 许可证

本项目采用 MIT 许可证。详见 [LICENSE](LICENSE) 文件。
